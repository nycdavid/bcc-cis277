#include <iostream>
#include <fstream>
#include "./vector.hpp"

int main() {
  // 3. Vector Addition
  // ------------------
  // Vector vct1 = get_vector();
  // Vector vct2 = get_vector();
  // Vector sum = { "Sum", 0, 0 };
  // calc_sum(vct1, vct2, sum);
  //
  // std::ofstream output_file;
  // output_file.open("output.txt");
  // print_vector(output_file, vct1);
  // print_vector(output_file, vct2);
  // print_vector(output_file, sum);
  // output_file.close();

  // 4. Vector Subtraction
  // ---------------------
  // Vector vct1 = get_vector();
  // Vector vct2 = get_vector();
  // Vector diff = { "Difference", 0, 0 };
  // calc_difference(vct1, vct2, diff);
  //
  // std::ofstream output_file;
  // output_file.open("output.txt");
  // print_vector(output_file, vct1);
  // print_vector(output_file, vct2);
  // print_vector(output_file, diff);
  // output_file.close();

  // 5. Scalar Multiple
  // ------------------
  // Vector vct1 = get_vector();
  // Vector scalar_multiple = { "Scalar Multiple", 0, 0 };
  // calc_scalar_multiple(vct1, scalar_multiple, 10);
  //
  // std::ofstream output_file;
  // output_file.open("output.txt");
  // print_vector(output_file, vct1);
  // print_vector(output_file, scalar_multiple);
  // output_file.close();

  // 6. Scalar Product
  // -----------------
  // Vector vct1 = get_vector();
  // Vector vct2 = get_vector();
  // double scalar_product;
  // calc_scalar_product(vct1, vct2, scalar_product);
  //
  // std::ofstream output_file;
  // output_file.open("output.txt");
  // output_file << "Scalar product = " << scalar_product << std::endl;
  // output_file.close();

  // 7. Vector Magnitude
  // -------------------
  // Vector vct1 = get_vector();
  // double magnitude;
  // calc_vector_magnitude(vct1, magnitude);
  //
  // std::ofstream output_file;
  // output_file.open("output.txt");
  // output_file << "Magnitude of " << vct1.name << " = " << magnitude << std::endl;
  // output_file.close();

  return 0;
}
